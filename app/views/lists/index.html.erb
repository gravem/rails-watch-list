<h1 class="display-3">All lists</h1>

<div class="mt-3">
  <% @lists.each do |list| %>
  <h3><%= link_to list.name, list_path(list) %></h3>
  <% end %>
</div>

<%= link_to 'add new list', new_list_path %>

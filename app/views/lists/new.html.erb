<h1>New list</h1>
<div class="container">
  <%= simple_form_for(@list) do |f| %>
    <%= f.input :name %>
    <%= f.submit %>
  <% end %>
</div>
<%= link_to 'Never mind... Take me back', lists_path %>
